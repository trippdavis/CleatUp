<h3 class="interests-header"></h3>

<br>

<div>
  <% interests.each(function (interest) { %>
    <button data-id="<%= interest.id %>" class="hidden-interest interest btn btn-default"><%= interest.escape("topic") %></button>
  <% }) %>
</div>

<br>
